# Runtime dependencies for the Streamlit dashboard (app/main.py) only.
# The dashboard reads the versioned Parquet files under data/marts/ and
# never imports the eco2mix package, so it does not need requests,
# duckdb, or python-dotenv — see SPEC.md's architecture decision that the
# dashboard never talks to the API or the warehouse directly.
streamlit>=1.40
pandas>=2.2
pyarrow>=16.0
altair>=5.0
